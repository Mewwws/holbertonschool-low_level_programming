#include "holberton.h"

/**
 * main - does nothing yet
 * @ac: arguments count
 * @av: arguments string pointer
 * Return: 0
 */
int main(int ac, char **av)
{
	if (ac == 1)
		printf("%s", av[1]);
	return (0);
}
