#include "holberton.h"

/**
 * _atoi - converts a string to an integer
 * @s: converted string
 * Return: inted string
 */
int _atoi(char *s)
{
	int c;
	int i;
	int j;
	int o;

	c = 0;
	while (s[c] != '\0')
	{
		c++;
	}
	char m[c];

	j = 0;
	for (i = 0; s[i] != '\0'; i++)
	{
		if ('0' <= s[i] && s[i] <= '9') && (!('0' <= s[i + 1]) && !(s [i + 1] <= '9'))
						   {
							   m[j] = 
						   }

	}



	o = 0;


	return (o);

}
